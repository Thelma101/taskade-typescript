"use strict";
// Literal types
let myName = "Tee";
let userName;
userName = 'Rachel';
// Functions
const add = (a, b) => {
    return a + b;
};
const logMsg = (message) => {
    console.log(message);
};
logMsg('Hello!');
logMsg(add(2, 39));
